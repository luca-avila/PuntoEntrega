# Public API base used at build time for production bundles.
# If Nginx proxies /api to the backend, keep this value as /api.
VITE_API_BASE_URL=/api

# Google Geocoding API key for production geocoding precision.
# Restrict this key by HTTP referrer to your production domain.
VITE_GOOGLE_MAPS_API_KEY=
